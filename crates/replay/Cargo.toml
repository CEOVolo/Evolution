[package]
name = "replay"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deterministic replay + golden-hash tool for sim-core."

[[bin]]
name = "replay"
path = "src/main.rs"

[dependencies]
sim-core = { workspace = true }
